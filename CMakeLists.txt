# For more information about build system see
# https://docs.espressif.com/projects/esp-idf/en/latest/api-guides/build-system.html
# The following five lines of boilerplate have to be in your project's
# CMakeLists in this exact order for cmake to work correctly
cmake_minimum_required(VERSION 3.16)

set(PROJECT_VER "2.2.3")

# Local project components under main/components
list(APPEND EXTRA_COMPONENT_DIRS "${CMAKE_CURRENT_LIST_DIR}/main/components")

# Add this line to disable the specific warning
add_compile_options(-Wno-missing-field-initializers)
add_compile_options(-Wno-error=format)
add_compile_options(-Wno-error=format-signedness)
add_compile_options(-Wno-error=format-truncation)
add_compile_options(-Wno-error=format-overflow)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(xiaozhi)
